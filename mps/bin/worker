#!/bin/bash

# MATLAB executes this script directly, hence we load the module here.
module load mps/r2023b

if [ "$1" = "-parallel" ]; then
    export MDCE_PARALLEL=1
    shift
fi

# Call matlab with the right arguments
matlab -parallelserver -nodisplay -r distcomp_evaluate_filetask "$@"
