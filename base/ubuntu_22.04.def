Bootstrap: docker
From: ubuntu:22.04

%help
    Base container for installing MATLAB.
    MATLAB requires `xorg` to run.
    We also install supported version of `gcc` and `gfortran` compilers.
    https://se.mathworks.com/support/requirements/supported-compilers-linux.html

%post
    apt-get --quiet update && \
    DEBIAN_FRONTEND=noninteractive \
    apt-get install -y xorg gcc-11 gfortran-10 && \
    rm -rf /var/lib/apt/lists/* && \
    ln -sf /bin/gcc-11 /bin/gcc && \
    ln -sf /bin/gfortran-10 /bin/gfortran
